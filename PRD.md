# 조건 기반 멀티-쇼핑몰 검색 어시스턴트 (ShopFinder)

**Version:** v1
**Status:** Draft

## 1. Product Overview

사용자가 색상·사이즈·소재·핏·가격 등 세부 조건을 자연어로 입력하면, 사전 설정된 여러 쇼핑몰을 자동으로 동시 검색해 조건에 맞는 상품 중 최저가·최적 매칭 결과를 모아서 보여주는 셀프호스팅 웹 도구. 라즈베리파이 4 (4GB)에서 Docker Compose로 동작하며, 사용자가 입력한 OpenAI 호환 엔드포인트를 통해 LLM을 활용해 자연어 조건 파싱, 사이트별 검색어 최적화, 결과 조건 일치도 점수화, 한 줄 추천 코멘트 생성을 수행한다. 기본 5개 쇼핑몰(쿠팡, 네이버 쇼핑, 11번가, G마켓, 무신사)이 사전 설정되어 있고, 사용자가 자유롭게 추가·삭제·활성화 토글할 수 있다.

## 2. Goals

### Business Goals
- 사용자가 1회 쇼핑 결정에 들이는 시간을 약 30분에서 5분 이내로 단축한다.
- 조건이 까다로워 비교 검색이 귀찮아 구매를 포기하던 케이스를 줄여, 셀프호스터 본인의 쇼핑 의사결정 만족도를 높인다.

### User Goals
- 옷처럼 종류·판매처가 많은 카테고리에서, 여러 쇼핑몰을 일일이 돌지 않고 한 번에 조건 만족 상품을 비교한다.
- 자연어 한 줄로 조건을 입력하고, 결과 사이에서 최저가·매칭 점수 기반으로 후보를 빠르게 좁힌다.
- 사용 중인 LLM 백엔드(OpenAI / Claude / 로컬 모델 등)와 검색 대상 쇼핑몰을 자유롭게 바꿔가며 자기 환경에 최적화한다.

### Non-Goals
- 실제 결제·장바구니 자동화 (도구는 검색·비교까지만 담당하며, 최종 구매 클릭은 사용자가 직접 수행).
- 해외 직구 사이트(Amazon, AliExpress, Taobao 등) 지원.
- 중고 거래 사이트(당근마켓, 번개장터 등) 지원.
- 가격 변동 추적·알림 기능 (이번 버전은 일회성 검색만).
- 별도 네이티브 모바일 앱 (반응형 웹으로 모바일 대응).
- 다중 사용자 계정/권한 시스템 (단일 셀프호스터 가정).

## 3. User Personas

### Key User Types
- 셀프호스터 (Self-hoster): 본인 및 가족·지인 정도가 사용하는 개인용 도구의 단일 운영자 겸 사용자.

### Basic Persona Details
- 40대 후반 남성, 도커와 라즈베리파이 셋업에 능숙.
- 가정 내 라즈베리파이 4 (4GB)에서 도구를 호스팅하고, 데스크톱·모바일 브라우저에서 비정기적으로(필요할 때만) 접근.
- 옷 등 종류와 판매처가 많은 품목을 살 때 조건 비교가 귀찮아 구매를 포기한 경험이 누적되어 있어, "조건 입력 → 결과 한눈에" 흐름을 가장 큰 가치로 본다.

### Role-Based Access
- Single role: 셀프호스터. 인증·권한 분리 없음. 외부 노출 시 단순 비밀번호 보호는 Phase 3 옵션으로 제공.

## 4. User Stories

- **US-1:** 셀프호스터로서, 자연어 한 줄로 색상·사이즈·소재·핏·가격 등의 조건을 입력해 한 번에 여러 쇼핑몰을 검색하고 싶다 — 사이트를 하나씩 돌지 않고 후보를 한꺼번에 보기 위해.
- **US-2:** 셀프호스터로서, 검색 결과를 가격순·조건 매칭 점수순으로 정렬·필터링해 보고 싶다 — 최저가와 베스트 매칭을 빠르게 판단하기 위해.
- **US-3:** 셀프호스터로서, 각 결과 카드에서 원본 쇼핑몰 상품 페이지로 바로 이동하고 싶다 — 결제는 직접 처리하기 위해.
- **US-4:** 셀프호스터로서, OpenAI 호환 LLM 엔드포인트(베이스 URL + API 키 + 모델명)를 UI에서 설정하고 싶다 — 원하는 LLM 백엔드를 자유롭게 사용하기 위해.
- **US-5:** 셀프호스터로서, 검색 대상 쇼핑몰을 추가·제거·활성화 토글하고 싶다 — 카테고리·취향에 맞게 검색 범위를 조정하기 위해.
- **US-6:** 셀프호스터로서, 라즈베리파이에서 동작하는 반응형 웹 UI로 데스크톱·모바일 어디서나 접근하고 싶다 — 폰·PC에서 같은 도구를 쓰기 위해.
- **US-7:** 셀프호스터로서, 각 결과 카드에서 상품 이미지·가격·핵심 스펙·"왜 이 조건과 매칭됐는지" 한 줄 코멘트를 보고 싶다 — 클릭 전에 판단하기 위해.

## 5. Functional Requirements

- **FR-1:** 자연어 조건 입력 폼을 단일 페이지 상단에 제공한다 (implements US-1).
- **FR-2:** 입력된 자연어를 구조화된 검색 파라미터(카테고리, 색상, 사이즈, 소재 및 비율, 핏, 최대 가격 등)로 파싱한다. LLM 설정 시 LLM 기반 파싱, 미설정 시 정규식·키워드 기반 폴백 파싱을 수행한다 (implements US-1, US-4).
- **FR-3:** 쇼핑몰별 검색을 추상화한 어댑터 인터페이스를 정의하고, 어댑터 등록·해제만으로 검색 대상이 추가·제거되도록 한다 (implements US-1, US-5).
- **FR-4:** 기본 5개 쇼핑몰 어댑터를 사전 탑재한다 — 쿠팡, 네이버 쇼핑, 11번가, G마켓, 무신사. 네이버 쇼핑은 공식 오픈API, 나머지 4개는 HTML 파싱(`httpx` + `selectolax`)으로 구현하며, Phase 3에서 Playwright 폴백을 옵션 제공한다 (implements US-1).
- **FR-5:** 활성화된 모든 쇼핑몰을 비동기로 병렬 검색하고, 각 쇼핑몰에서 결과가 들어오는 대로 Server-Sent Events로 점진적으로 UI에 스트리밍한다 (implements US-1).
- **FR-6:** 결과 카드는 상품 이미지 썸네일, 상품명, 가격, 쇼핑몰명, 핵심 스펙, 그리고 LLM이 생성한 "조건 매칭 이유" 한 줄 코멘트를 표시한다. LLM 미설정 시 "조건 매칭 이유"는 비표시 (implements US-7).
- **FR-7:** 결과를 가격 오름차순, 가격 내림차순, 조건 매칭 점수 내림차순, 쇼핑몰별 그룹으로 정렬·필터링할 수 있다 (implements US-2).
- **FR-8:** 각 결과 카드 클릭 시 원본 쇼핑몰 상품 페이지를 새 탭에서 연다 (implements US-3).
- **FR-9:** 설정 페이지에서 OpenAI 호환 LLM 엔드포인트의 베이스 URL, API 키, 모델명을 입력·저장할 수 있다. API 키는 SQLite에 저장하되 평문 노출은 마스킹 처리한다 (implements US-4).
- **FR-10:** LLM 엔드포인트가 미설정·연결 실패 시 도구는 정규식 기반 파싱만으로 정상 동작하며, LLM 의존 기능(사이트별 검색어 최적화, 조건 매칭 점수, 추천 코멘트)은 자동으로 비활성화된다 (implements US-4).
- **FR-11:** 쇼핑몰 관리 페이지에서 사전 탑재 5개 + 사용자 추가 어댑터를 활성/비활성 토글하고, 어댑터 추가·삭제를 GUI에서 수행할 수 있다. 사용자 정의 어댑터 추가의 기술적 상세(예: 어댑터 정의 포맷)는 Phase 3에서 확정 — _TBD_ (implements US-5).
- **FR-12:** 웹 UI는 모바일·데스크톱 둘 다 사용 가능한 반응형 디자인으로 제공한다 (implements US-6).
- **FR-13:** 동일 조건(파싱된 파라미터 해시 기준) 검색은 24시간 동안 캐시된 결과를 반환하고, 사용자가 명시적으로 "새로고침"을 선택하면 강제 재검색한다.
- **FR-14:** 모든 검색 요청과 결과 메타데이터(조건, 응답 시간, 결과 수, 사용자가 클릭한 상품)를 SQLite에 영구 저장하고, 히스토리 페이지에서 조회·재실행할 수 있다.
- **FR-15:** LLM이 파싱한 조건은 검색창 아래에 가격·사이즈·색상·소재 등 편집 가능한 필드로 표시되며, 사용자가 수동으로 보정 후 재검색할 수 있다 (implements US-1).
- **FR-16:** LLM 호출을 제외한 모든 데이터(검색 히스토리, 설정, 캐시)는 로컬 SQLite에만 저장되고 외부로 전송되지 않는다. 외부 통신은 (a) 사용자 지정 LLM 엔드포인트, (b) 5개 쇼핑몰 검색 호출 두 가지로 한정된다.
- **FR-17:** Phase 3에서 외부 노출 시 사용할 수 있는 단순 비밀번호 보호(베이직 인증 또는 단일 비밀번호 세션)를 옵션 기능으로 제공한다.

## 6. User Experience

### Entry Points & First-Time User Flow
- 사용자는 라즈베리파이의 호스트(예: `http://raspberrypi.local:8080`)로 브라우저에서 접속한다.
- 최초 진입 시 환영 화면이 LLM 엔드포인트 설정을 권장하되, 미설정 상태로도 즉시 검색이 가능함을 안내한다.
- 설정 페이지에서 OpenAI 호환 베이스 URL, API 키, 모델명을 입력하면 "연결 테스트" 버튼으로 즉시 검증한다.
- 기본 5개 쇼핑몰은 모두 활성화된 상태로 제공된다.

### Core Experience
- **Step 1 — 자연어 입력:** 상단 검색창에 "검정색 100 사이즈 긴팔 남방 폴리에스테르 80 이상 루즈핏 2만원 이하" 같은 한 줄을 입력한다.
- **Step 2 — 파싱 결과 확인:** 검색창 아래에 LLM(또는 폴백 파서)이 추출한 카테고리·색상·사이즈·소재/비율·핏·최대 가격이 편집 가능한 필드로 표시된다. 잘못 잡힌 값은 직접 보정 가능.
- **Step 3 — 검색 실행:** "검색" 버튼을 누르면 활성화된 모든 쇼핑몰에 병렬로 요청이 나가고, 결과가 도착하는 대로 화면 하단의 카드 그리드에 스트리밍으로 추가된다. 각 쇼핑몰의 로딩 상태(검색 중·완료·실패)가 상단에 표시된다.
- **Step 4 — 비교·정렬:** 결과는 가격 오름차순이 기본. 매칭 점수순·쇼핑몰별 그룹·최대가격 필터 등으로 즉시 재정렬한다.
- **Step 5 — 외부 이동:** 카드를 클릭하면 원본 쇼핑몰 상품 페이지가 새 탭에서 열린다. 사용자가 이 액션을 수행한 사실은 히스토리에 기록된다.
- **Step 6 — 재검색·캐시:** 같은 조건 재검색은 24시간 내 캐시 결과를 즉시 반환하며, "새로고침"으로 강제 재실행 가능.

### Advanced Features & Edge Cases
- **쇼핑몰 추가/제거:** 설정 페이지에서 기본 5개 + 사용자가 추가한 어댑터를 토글하거나 삭제. 사용자 정의 어댑터 정의 방식은 Phase 3에서 확정 (_TBD_).
- **LLM 미설정 모드:** LLM이 비어 있거나 연결 실패 시 정규식 파싱만으로 동작하고, 매칭 점수·추천 코멘트는 카드에 표시되지 않는다.
- **쇼핑몰별 실패 처리:** 한 쇼핑몰이 타임아웃·HTML 구조 변경 등으로 실패해도 전체 검색은 진행되며, 해당 쇼핑몰만 "실패" 상태로 표시되고 다른 결과는 정상적으로 스트리밍된다.
- **HTML 구조 변경 감지:** 어댑터가 0건 또는 파싱 실패를 반복하면 히스토리에 경고 로그를 남겨 셀프호스터가 어댑터 점검 시점을 알 수 있게 한다.
- **속도 제어:** 동일 쇼핑몰에 대해 동시 요청 1개로 제한하고, 짧은 간격의 연속 검색에는 무작위 지연을 추가해 IP 차단 위험을 줄인다.
- **LLM 호출 정책:** 기본 배포가 LM Studio 로컬이므로 비용 제약 없음. 사용 빈도를 자유롭게 두고, 결과 매칭 점수·코멘트 생성도 결과 카드 단위로 모두 호출한다. 외부 유료 API를 가리키는 경우를 대비해 "검색 1회당 LLM 호출 상한"은 선택적 옵션으로 남기되 기본값은 무제한.

### UI/UX Highlights
- 하이브리드 검색 UI — 상단에 자연어 검색창 + 아래에 LLM이 파싱한 결과를 편집 가능한 필드로 노출.
- 결과 카드 그리드는 모바일에서 1열, 태블릿 2열, 데스크톱 3~4열로 반응형 변환.
- SSE 기반 스트리밍으로 첫 결과 도착 시점을 빠르게 체감하도록 한다(전체 완료를 기다리지 않는다).
- 라즈베리파이의 자원 제약을 고려해 클라이언트사이드 빌드를 최소화하고, HTMX + Jinja 템플릿으로 서버 렌더링을 기본으로 한다.

## 7. Narrative

40대 후반 셀프호스터인 사용자는 늦은 밤 거실 소파에서 폰을 들고, 평소 입던 검정색 긴팔 남방을 새로 하나 사야겠다고 마음먹는다. 평소라면 쿠팡 → 네이버쇼핑 → 무신사로 옮겨 다니며 색상·사이즈·소재 비율을 일일이 필터링하다 30분쯤 지나 짜증이 나서 구매를 포기했을 일이다.

이번에는 거실 라즈베리파이에서 돌아가는 ShopFinder에 접속해 검색창에 한 줄을 친다 — "검정색 100 긴팔 남방 폴리에스테르 80 이상 루즈핏 2만원 이하". 아래 보정 필드에 조건이 정확히 들어가 있는 걸 확인하고 검색 버튼을 누른다. 5개 쇼핑몰의 로딩 표시가 순서대로 켜지고, 몇 초 만에 첫 카드가 도착하더니 30초 안에 카드 그리드가 가격순으로 정렬된 채 가득 찬다. 각 카드 옆 한 줄 코멘트("폴리에스테르 82%, 루즈핏 표기, 최저가")를 훑고, 가장 마음에 드는 무신사 카드를 눌러 원본 페이지에서 결제한다. 5분 만에 끝났고, 다음에 같은 조건을 또 입력하면 캐시된 결과가 즉시 뜬다.

## 8. Success Metrics

| Metric | Type | Baseline | Target | Timeframe |
|---|---|---|---|---|
| 1회 쇼핑 결정에 드는 시간 (검색 시작 → 구매 클릭) | Business | ~30분 (자가 추정) | ≤5분 | 사용 첫 달 내 안정화 |
| 조건 입력 → 첫 결과 도착까지 응답 시간 | User | _TBD_ | ≤5초 (P50) | 출시 시점 |
| 5개 쇼핑몰 병렬 검색 전체 완료 시간 | User | _TBD_ | ≤30초 (P95) | 출시 시점 |
| 검색당 평균 쇼핑몰 커버리지 (정상 응답 / 활성 쇼핑몰 수) | User | _TBD_ | ≥80% | 출시 후 첫 달 |
| 사용자가 결과 카드를 실제로 클릭해 원본 페이지로 이동한 검색의 비율 | User | _TBD_ | ≥50% | 출시 후 첫 달 |
| 라즈베리파이 4 (4GB) 상의 정상 동작 시 메모리 사용량 | Technical | 0 (greenfield) | ≤1.0GB RSS | 출시 시점 |
| 검색 1회 LLM 추론 총 시간 (LM Studio 로컬 기준) | Technical | _TBD_ | ≤10초 (전체 응답 30초 내 비중) | 출시 시점 |
| 쇼핑몰 어댑터 실패율 (HTTP/HTML 파싱 에러) | Technical | 0 (greenfield) | ≤5% per adapter per day | 출시 후 첫 달 |
| 셋업 후 사용자가 쇼핑몰·LLM 설정을 추가로 조정한 횟수 (정성 지표) | User | _TBD_ | 사용자가 원하는 만큼 자유롭게 조정 가능 | 상시 |

## 9. Technical Considerations

### Integration Points
- **LLM:** 사용자 지정 OpenAI 호환 엔드포인트 (베이스 URL + API 키 + 모델명). **기본 배포 가정은 LM Studio 로컬 인스턴스 — 라즈베리파이 외 다른 머신(데스크톱·홈 서버)에서 LM Studio를 띄우고 그 LAN endpoint를 가리킨다.** 호환 가능한 백엔드는 LM Studio, Ollama, vLLM, OpenAI API, Anthropic Claude 호환 프록시 등. 로컬 LLM이 기본이므로 토큰 비용은 제약 사항이 아니며, LLM 호출을 자유롭게 사용한다.
- **쇼핑몰 1 — 네이버 쇼핑:** 공식 오픈API (검색 API, `https://openapi.naver.com/v1/search/shop.json`). 클라이언트 ID/Secret 필요, 설정 페이지에서 입력.
- **쇼핑몰 2~5 — 쿠팡 / 11번가 / G마켓 / 무신사:** 검색 결과 페이지 HTML 파싱 (`httpx` + `selectolax`). 각 어댑터는 검색 URL 패턴과 결과 셀렉터를 책임진다.
- **Playwright 폴백 (Phase 3):** JS 렌더링이 필요한 어댑터를 위한 옵션 컴포넌트. ARM64 Playwright 이미지 사용.

### Data Storage & Privacy
- **저장소:** 단일 SQLite 파일. 테이블은 대략 `settings`, `shops`, `search_history`, `search_results_cache`, `click_log`.
- **민감 데이터:** 네이버 API 키, OpenAI 호환 LLM API 키. 평문으로 SQLite에 저장하되 UI에서는 마스킹. 라즈베리파이 내부에서만 접근됨을 전제로 한다.
- **외부 전송 범위:** (a) 사용자 지정 LLM 엔드포인트 호출, (b) 5개 쇼핑몰 검색 호출. 이 외 외부 텔레메트리·분석 호출 없음.
- **검색 히스토리:** 영구 저장. 사용자가 히스토리 페이지에서 개별 삭제 또는 전체 비우기 가능 (Phase 2 내 포함 권장).

### Scalability & Performance
- **타깃 하드웨어:** 라즈베리파이 4 (4GB RAM, ARM64). 단일 사용자 동시성 1을 가정.
- **메모리 예산:** Python 프로세스 1.0GB 이하. Playwright는 옵션이며 활성화 시 추가 ~300~500MB 예상.
- **동시성:** `asyncio` 기반 비동기 HTTP. 어댑터당 최대 동시 요청 1개, 전체 동시 요청 5개(쇼핑몰 수와 일치).
- **응답 시간 목표:** 첫 결과 P50 5초 이하, 전체 P95 30초 이하.
- **캐시 전략:** 24시간 TTL의 결과 캐시. 캐시 키는 파싱된 정규화 조건의 해시.
- **빌드 결과물:** 단일 Docker 이미지(ARM64 멀티아키 빌드 권장), Docker Compose 1줄 실행.

### Potential Challenges
- **HTML 구조 변경:** 4개 사이트의 마크업이 수시로 바뀐다. 어댑터 실패율 메트릭과 경고 로그로 조기 감지가 필수.
- **봇 차단:** User-Agent, 요청 간격, IP 차단 위험. 동일 쇼핑몰에 동시 1요청 제한 + 무작위 지연 + 표준 브라우저 User-Agent 사용.
- **LLM 지연:** 기본 배포가 LM Studio 로컬이므로 비용은 비제약이지만, 로컬 LLM 추론 속도가 검색 전체 응답 시간(P95 30초)의 병목이 될 수 있다. 호출을 병렬화하고, 결과 매칭 점수는 결과 N건 단위로 배치 호출해 왕복 횟수를 줄인다. LM Studio가 도는 머신의 GPU/CPU 사양이 응답 품질·속도를 좌우.
- **Pi 4 ARM64 호환성:** 일부 Python 패키지·Playwright 브라우저의 ARM 빌드 가용성 확인 필요.
- **사용자 정의 어댑터 정의 포맷:** Phase 3에서 결정 — 선택지로 (a) Python 클래스 + 드롭인 디렉토리, (b) YAML 선언형(셀렉터 + URL 패턴) — _TBD_.
- **법적·이용약관:** 일부 쇼핑몰은 스크래핑이 이용약관에 저촉될 수 있다. 개인용·비공개 셀프호스팅 범위 내에서 사용함을 전제로 한다.

## 10. Build Phases

### Phase 1 — Scaffolding & Core Search
- FastAPI + Jinja + HTMX + SQLite 기반 프로젝트 구조 셋업.
- Docker Compose 단일 명령 실행 환경 (ARM64 이미지 빌드 포함).
- 쇼핑몰 어댑터 인터페이스 정의 + 네이버 쇼핑 공식 API 어댑터 1개 구현.
- 자연어 입력 → 정규식·키워드 기반 조건 파싱 (LLM 없이도 검색 동작).
- 단일 페이지 웹 UI + SSE로 결과 스트리밍.
- 검색 히스토리 SQLite 저장 + 히스토리 페이지 기본 조회.

### Phase 2 — Multi-Shop + LLM Integration
- 쿠팡·11번가·G마켓·무신사 HTML 파싱 어댑터 4종 구현.
- 설정 페이지에서 OpenAI 호환 LLM 엔드포인트(베이스 URL + API 키 + 모델명) 입력 및 연결 테스트.
- LLM 기반 4기능 통합 — 조건 파싱, 사이트별 검색어 최적화, 결과 조건 매칭 점수화, 추천 코멘트 생성.
- 결과 카드 매칭 점수·추천 코멘트 표시.
- 24시간 결과 캐시 + 강제 재검색.
- 어댑터 실패·HTML 변경 감지 경고 로그.
- 검색 1회당 LLM 호출 상한(옵션, 기본 무제한 — LM Studio 로컬 가정).

### Phase 3 — Polish & Extensibility
- 쇼핑몰 추가/삭제 UI + 사용자 정의 어댑터 정의 포맷 확정 및 지원.
- LLM 파싱 결과 보정 UI (편집 후 재검색).
- 모바일 반응형 디테일 마감.
- Playwright 폴백 옵션 (ARM64) — JS 렌더링 필요한 어댑터용.
- 외부 노출 시 단순 비밀번호 보호 (베이직 인증 또는 단일 비밀번호 세션) 옵션.
- 히스토리 페이지 검색·필터·재실행·삭제 기능 강화.
